meta {
  name: Set a Security Group as a default for running Apps
  type: http
  seq: 3
}

put {
  url: {{base_url}}/v2/config/running_security_groups/:guid
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Set a Security Group as a default for running Apps
}
