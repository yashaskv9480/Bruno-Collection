meta {
  name: Update contents of the staging environment variable group
  type: http
  seq: 4
}

put {
  url: {{base_url}}/v2/config/environment_variable_groups/staging
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Update contents of the staging environment variable group
}
