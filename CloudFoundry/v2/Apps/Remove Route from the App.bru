meta {
  name: Remove Route from the App
  type: http
  seq: 15
}

delete {
  url: {{base_url}}/v2/apps/:guid/routes/:route_guid
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Remove Route from the App
}
