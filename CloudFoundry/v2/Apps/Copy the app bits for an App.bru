meta {
  name: Copy the app bits for an App
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v2/apps/:guid/copy_bits
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Copy the app bits for an App
}
