meta {
  name: Remove Service Binding from the App
  type: http
  seq: 16
}

delete {
  url: {{base_url}}/v2/apps/:guid/service_bindings/:service_binding_guid
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Remove Service Binding from the App
}
