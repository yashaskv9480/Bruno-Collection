meta {
  name: Retrieve key credential.
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/credentials/key?name=
  body: none
  auth: inherit
}

params:query {
  name: 
}

headers {
  sapcp-credstore-namespace: 
  ~If-None-Match: 
}

script:post-response {
  const tools = require('./scripts/credstore-functions');
  await tools.decryptPayload(res.body);
  
}
